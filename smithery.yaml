# Smithery configuration file: https://smithery.ai/docs/build/project-config/smithery-yaml

startCommand:
  type: stdio
  configSchema:
    type: object
  commandFunction:
    |-
    (config) => ({ command: 'npx', args: ['-y', '@vibecheck-mcp/mcp'] })
